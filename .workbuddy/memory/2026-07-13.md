# 2026-07-13 工作日志

## EdgeOne Makers Skills 安装

从 GitHub 仓库 `https://github.com/TencentEdgeOne/edgeone-pages-skills` 安装了 EdgeOne Makers 技能包。

### 安全审查结果
- 仓库为腾讯 EdgeOne 官方仓库，MIT 许可证
- 审查了所有 SKILL.md、hooks 脚本、scripts 脚本
- hooks: `validate-write.mjs` (文件写入验证) 和 `signal-log.mjs` (信号日志) — 无恶意行为
- scripts: `build.mjs` (构建脚本) 和 `archive-traffic.mjs` (GitHub Actions 流量归档) — 无安全风险
- 评估结果: **安全 (P2)**

### 安装的技能 (10 个)
1. `makers-agents` — AI Agent 开发 (DeepAgents, LangGraph, Claude SDK, OpenAI Agents, CrewAI)
2. `makers-cli` — CLI 命令参考
3. `makers-cloud-functions` — 云函数 (Node.js / Go / Python)
4. `makers-deploy` — 部署工作流
5. `makers-edge-functions` — 边缘函数 (V8 运行时)
6. `makers-env-adaption` — 环境适配 (WorkBuddy / 沙箱 / CI)
7. `makers-middleware` — 中间件 (认证、重写、路由)
8. `makers-migration` — 迁移指南
9. `makers-recipes` — 项目模板与脚手架
10. `makers-storage` — KV + Blob 存储

### 安装位置
`~/.workbuddy/skills/` (用户级，跨项目可用)

## 海龟汤项目部署到 EdgeOne Makers

### 部署结果
- **线上地址**: `https://turtle-soup-ai-em1n2z3s.edgeone.cool`
- **Project ID**: `makers-xqvruhzexqtq`
- **Console URL**: `https://console.cloud.tencent.com/edgeone/pages/project/makers-xqvruhzexqtq`

### 项目重构内容
1. 创建 `cloud-functions/api/[[default]].js` — 将 Express API 转为 Cloud Functions（内嵌题库、AI 客户端、SSE 流式、所有 REST 路由）
2. 修改根 `package.json` — 添加 express 依赖，build 脚本改为仅构建 client
3. 修复 `client/src/types/game.ts` — 类型导入从 `export { type X } from` 改为 `import type + export type`

### 部署适配限制
- **多人模式 (Socket.io)**: 不可用 — serverless 环境不支持持久 WebSocket 连接
- **AI 功能**: ✅ 已启用 — 用户在 EdgeOne 控制台配置了 DeepSeek 环境变量（`AI_API_KEY`、`AI_BASE_URL=https://api.deepseek.com/v1`、`AI_MODEL=deepseek-chat`），重新部署后生效
- **会话状态**: 内存态，函数实例回收后可能丢失
- **投稿/审核**: 内存态存储，不持久化

## Cloud Studio 部署（永久链接，用于黑客松提交）

### 背景
EdgeOne Makers 临时链接仅 3 小时有效，不适合黑客松参赛提交。改用 Cloud Studio 部署全静态版本。

### 重构内容
- 将 `client/src/api.ts` 完全重写为客户端版本：
  - 9 道题库数据内联到前端代码
  - AI 调用直接从浏览器 fetch 到 DeepSeek API（OpenAI 兼容格式，支持流式）
  - 会话管理用 localStorage（代替服务端内存）
  - AI 配置（API Key / baseURL / model）存储在 localStorage，用户在设置页面输入
  - 投稿/审核数据存储在 localStorage
- 保持所有接口签名不变，前端组件无需修改

### 部署结果
- **Cloud Studio 永久链接**: `https://6f2c9a3763c342ccb0da15412762c6b3.app.codebuddy.work`
- 部署目录: `client/dist/`
- HTTP 200 验证通过

### 注意事项
- AI 功能需用户首次访问时在设置页面输入 DeepSeek API Key
- 多人模式（Socket.io）不可用
- API Key 存储在浏览器 localStorage（每设备独立）
